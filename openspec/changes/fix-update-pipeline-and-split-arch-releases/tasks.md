## 1. 统一应用内更新入口

- [ ] 1.1 扩展 `UpdateManager`，统一暴露手动检查更新、启动时检查更新和运行时可用状态
- [ ] 1.2 将更新偏好设置页与主菜单都接入统一的更新入口，并补充本地化文案
- [ ] 1.3 为更新入口和菜单行为补充单元测试与 UI 冒烟测试

## 2. 改造分架构发布链路

- [ ] 2.1 调整 `build_dmg.sh` 支持 `arm64` / `x86_64` 单架构构建和架构后缀产物命名
- [ ] 2.2 调整 `generate_appcast.sh` 与 `publish_github_release.sh` 支持分架构资产与分架构 feed
- [ ] 2.3 确保分发构建写入对应架构的 `SUFeedURL` 并保留 Sparkle `enclosure` 签名字段

## 3. 验证与发布整理

- [ ] 3.1 验证更新设置页、主菜单和启动时检查更新行为
- [ ] 3.2 验证双架构构建产物、bundle 元数据和 appcast 输出
- [ ] 3.3 更新 OpenSpec 任务状态并准备下一个修复版发布说明
